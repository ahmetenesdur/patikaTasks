export default name => console.log(`Hello ${name}`);

const topla = (a, b) => a + b;
const cikar = (a, b) => a - b;

const text = 'Text';
const user = { name: 'Ahmet Enes', surname: 'Dur' };
const users = ['Ahmet Enes', 'Dur'];

export { topla, cikar, text, user, users };